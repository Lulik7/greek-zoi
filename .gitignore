# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# База данных: личные данные учеников и пароли — в историю версий не попадают
server/data

# Загруженные материалы. Оформление первого блока (картинка и видео) хранится
# вместе с кодом, а тяжёлые аудиофайлы уроков — нет
server/uploads/*
!server/uploads/hero-greek2.jpg
!server/uploads/hero-athlete.mp4
# Видео первого экрана — его запрашивает главная страница, без него там дыра
!server/uploads/hero-style-scene-hd.mp4

# Секреты
.env
.env.local

.tools/

# Служебная страница для просмотра сайта в телефонной ширине.
# Нужна только при разработке, в продакшен ей попадать незачем.
public/__phone.html
